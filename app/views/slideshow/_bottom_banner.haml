#bottomBanner
  = link_to "About",  root_path(:show_detail => true)
  |
  - if @bottom_links.present?
    - for link_name in @bottom_links
      = render_bottom_link link_name
      |
  %a#keyShortcutsLink{href: '#'}
    Key Shortcuts
  .copyrights
    = render partial: 'general/copyright'

  #debugInfo.inline

= render partial: 'key_shortcuts'