%h1 Listing flickr_streams

%p{:id => "notice"}
  = notice

%table
  %tr
    %th Type
    %th User
    %th Last sync
    %th
    %th
    %th
  - @flickr_streams.each do |flickr_stream|
    %tr
      %td= flickr_stream.type
      %td= link_to flickr_stream.username, flickr_stream.user_url
      %td= flickr_stream.last_sync ? flickr_stream.last_sync .getlocal.to_s(:short) : ''
      %td= link_to 'Sync', sync_flickr_stream_path(flickr_stream.id)
      %td= link_to 'Edit', edit_flickr_stream_path(flickr_stream.id)
      %td= link_to 'Destroy', flickr_stream, :confirm => 'Are you sure?', :method => :delete
%br/
= link_to 'New Flickr Stream', new_flickr_stream_path
|
= link_to 'Sync All Streams', sync_all_flickr_streams_path
|
= link_to 'Pictures', pictures_path