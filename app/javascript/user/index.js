/**
 * User Module
 * Handles user profile and preferences functionality
 */

// Define user namespace
window.klekr = window.klekr || {};
window.klekr.User = window.klekr.User || {};

// Export the user namespace
export default window.klekr.User;